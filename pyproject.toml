[project]
name = "ai-paper-radar"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "aws-cdk-lib>=2.252.0",
    "constructs>=10.0.0,<11.0.0",
]

[dependency-groups]
dev = [
    "moto>=5.0",
    "mypy>=1.20.2",
    "pytest>=9.0.3",
    "pytest-cov>=7.1.0",
    "requests-mock>=1.12.1",
    "ruff>=0.15.12",
]
